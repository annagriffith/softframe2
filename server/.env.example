## Backend environment variables

# Server port
PORT=3001

# MongoDB connection (set these to use a real Mongo instance)
MONGO_URL=mongodb://localhost:27017
DB_NAME=frametry

# JWT secret for signing tokens
JWT_SECRET=replace_with_strong_secret

# Development fallbacks:
# Force using mongodb-memory-server (downloads Mongo binaries once)
# USE_INMEMORY_DB=true
# If real Mongo connect fails, allow fallback to memory
# ALLOW_INMEMORY_FALLBACK=true
